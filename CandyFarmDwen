local Rayfield = loadstring(game:HttpGet('https://sirius.menu/rayfield'))()

-- 1. ADIM: ANTİ-AFK SİSTEMİ (SCRIPT AÇILDIĞI AN AKTİF OLUR)
local VirtualUser = game:GetService("VirtualUser")
game:GetService("Players").LocalPlayer.Idled:Connect(function()
    VirtualUser:CaptureController()
    VirtualUser:ClickButton2(Vector2.new())
end)

-- 2. ADIM: ANA PENCERE (KEY SİSTEMİ KAPALI)
local Window = Rayfield:CreateWindow({
   Name = "DwenElChavo | DwenFPS Candy Farm",
   LoadingTitle = "DwenFPS Yükleniyor...",
   LoadingSubtitle = "by DwenElChavo",
   ConfigurationSaving = {
      Enabled = true,
      FolderName = "DwenFPS_Config",
      FileName = "CandyFarm"
   },
   KeySystem = false -- Key sistemini tamamen kapattım kral
})

-- 3. ADIM: CANDY FARM ÖZELLİKLERİ
local MainTab = Window:CreateTab("Main Farm", 4483362458)
local running = false
local yukseklikAyari = 4 

-- En yakın yumurtayı bulan fonksiyon
local function enYakinYumurtayiBul()
    local char = game.Players.LocalPlayer.Character
    if not char or not char:FindFirstChild("HumanoidRootPart") then return nil end
    local enYakin = nil
    local enKisaMesafe = math.huge
    for _, v in pairs(workspace:GetDescendants()) do
        if v.Name == "CandyEgg" and (v:IsA("BasePart") or v:IsA("Model")) then
            local pos = (v:IsA("Model") and v:GetModelCFrame().Position) or v.Position
            local mesafe = (char.HumanoidRootPart.Position - pos).Magnitude
            if mesafe < enKisaMesafe then
                enKisaMesafe = mesafe
                enYakin = v
            end
        end
    end
    return enYakin
end

-- Farm Toggle (Aç/Kapat)
MainTab:CreateToggle({
   Name = "Candy AutoFarm",
   CurrentValue = false,
   Callback = function(Value)
      running = Value
      local char = game.Players.LocalPlayer.Character
      if running and char and char:FindFirstChild("HumanoidRootPart") then 
          task.spawn(function()
              while running do
                  local hedef = enYakinYumurtayiBul()
                  if hedef then
                      local pos = (hedef:IsA("Model") and hedef:GetModelCFrame().Position) or hedef.Position
                      char.HumanoidRootPart.CFrame = CFrame.new(pos + Vector3.new(0, yukseklikAyari, 0))
                  end
                  task.wait(0.1)
              end
          end)
      end
   end,
})

-- Uçuş Yüksekliği Ayarı
MainTab:CreateSlider({
   Name = "Uçuş Yüksekliği",
   Range = {0, 10},
   Increment = 1,
   CurrentValue = 4,
   Callback = function(Value) yukseklikAyari = Value end,
})

-- Bilgi Etiketleri
MainTab:CreateSection("Sistem Durumu")
MainTab:CreateLabel("Anti-AFK: Aktif ✅")
MainTab:CreateLabel("DwenFPS Keyless Sürüm")

Rayfield:Notify({Title = "Sistem Hazır", Content = "Hile başarıyla yüklendi, iyi oyunlar!", Duration = 5})
