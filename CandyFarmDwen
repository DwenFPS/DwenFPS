local Rayfield = loadstring(game:HttpGet('https://sirius.menu/rayfield'))()

-- ##########################################
-- 1. ADIM: REKLAM LİNKİNİ BURAYA YAPIŞTIR
-- ##########################################
local lootLink = "https://loot-link.com/SANA_OZEL_LINK" 

-- ##########################################
-- 2. ADIM: MANUEL GET KEY BUTONU (PROFESYONEL)
-- ##########################################
local ScreenGui = Instance.new("ScreenGui")
local Button = Instance.new("TextButton")
local UICorner = Instance.new("UICorner")
local UIStroke = Instance.new("UIStroke")

ScreenGui.Name = "DwenGetKeyGui"
ScreenGui.Parent = game.CoreGui
ScreenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
ScreenGui.DisplayOrder = 999 

Button.Name = "GetKeyButton"
Button.Parent = ScreenGui
Button.BackgroundColor3 = Color3.fromRGB(45, 45, 45)
Button.Position = UDim2.new(0.5, -60, 0.5, 65) 
Button.Size = UDim2.new(0, 120, 0, 30)
Button.Font = Enum.Font.GothamBold
Button.Text = "GET KEY"
Button.TextColor3 = Color3.fromRGB(255, 255, 255)
Button.TextSize = 14.000
Button.AutoButtonColor = true

UICorner.CornerRadius = UDim.new(0, 6)
UICorner.Parent = Button

UIStroke.Color = Color3.fromRGB(100, 100, 100)
UIStroke.Thickness = 1
UIStroke.Parent = Button

Button.MouseButton1Click:Connect(function()
    setclipboard(lootLink)
    Rayfield:Notify({Title = "Başarılı", Content = "Reklam linki panoya kopyalandı!", Duration = 5})
end)

-- ##########################################
-- 3. ADIM: RAYFIELD PENCERE VE KEY SİSTEMİ
-- ##########################################
local Window = Rayfield:CreateWindow({
   Name = "DwenElChavo Auto Candy Farm",
   LoadingTitle = "DwenElChavo CandyFarm",
   LoadingSubtitle = "Key System Aktif",
   ConfigurationSaving = {Enabled = false},
   Discord = {Enabled = false},
   KeySystem = true,
   KeySettings = {
      Title = "DwenElChavo | Key System",
      Subtitle = "Lütfen anahtarı aşağıya giriniz",
      Note = "GET KEY butonuna basarak reklamı geç!",
      FileName = "DwenKeyConfig",
      SaveKey = true,
      GrabKeyFromSite = true,
      Key = {"https://api.lootlabs.gg/v1/check-key/a880ca40634ca5ebfa765f9038556470ac41c46f40bab95b047964d25fb3d0de"}
   }
})

-- KEY DOĞRU GİRİLDİĞİNDE BUTONU YOK ET
ScreenGui:Destroy()

-- ##########################################
-- 4. ADIM: HİLE İÇERİĞİ VE FONKSİYONLAR
-- ##########################################
local MainTab = Window:CreateTab("Candy Farm", 4483362458)
local running = false
local antiAfkEnabled = true
local startPos = nil
local baslangicZamani = os.time()
local yukseklikAyari = 4 
local hizAyari = 0.1 

-- ANTI-AFK SİSTEMİ
local VirtualUser = game:GetService("VirtualUser")
game:GetService("Players").LocalPlayer.Idled:Connect(function()
    if antiAfkEnabled then
        VirtualUser:CaptureController()
        VirtualUser:ClickButton2(Vector2.new())
    end
end)

-- GHOST MODU (DUVARLARDAN GEÇME)
local function ghostMode(state)
    local char = game.Players.LocalPlayer.Character
    if not char or not char:FindFirstChild("HumanoidRootPart") then return end
    if state then
        for _, v in pairs(char:GetDescendants()) do
            if v:IsA("BasePart") then v.CanCollide = false v.CanTouch = false end
        end
        local bp = char.HumanoidRootPart:FindFirstChild("DwenFloat") or Instance.new("BodyVelocity")
        bp.Name = "DwenFloat"
        bp.Parent = char.HumanoidRootPart
        bp.Velocity = Vector3.new(0, 0, 0)
        bp.MaxForce = Vector3.new(math.huge, math.huge, math.huge)
        
        local bg = char.HumanoidRootPart:FindFirstChild("DwenGyro") or Instance.new("BodyGyro")
        bg.Name = "DwenGyro"
        bg.Parent = char.HumanoidRootPart
        bg.MaxTorque = Vector3.new(math.huge, math.huge, math.huge)
        bg.CFrame = startPos or char.HumanoidRootPart.CFrame
    else
        if not running then
            if char.HumanoidRootPart:FindFirstChild("DwenFloat") then char.HumanoidRootPart.DwenFloat:Destroy() end
            if char.HumanoidRootPart:FindFirstChild("DwenGyro") then char.HumanoidRootPart.DwenGyro:Destroy() end
            for _, v in pairs(char:GetDescendants()) do
                if v:IsA("BasePart") then v.CanCollide = true v.CanTouch = true end
            end
        end
    end
end

-- EN YAKIN CANDY BULUCU
local function enYakinYumurtayiBul()
    local char = game.Players.LocalPlayer.Character
    if not char or not char:FindFirstChild("HumanoidRootPart") then return nil end
    local enYakin = nil
    local enKisaMesafe = math.huge
    local eggs = workspace:GetDescendants()
    for i = 1, #eggs do
        local v = eggs[i]
        if v.Name == "CandyEgg" and (v:IsA("BasePart") or v:IsA("Model")) then
            local pos = (v:IsA("Model") and v:GetModelCFrame().Position) or v.Position
            local mesafe = (char.HumanoidRootPart.Position - pos).Magnitude
            if mesafe < enKisaMesafe then
                enKisaMesafe = mesafe
                enYakin = v
            end
        end
    end
    return enYakin
end

-- ARAYÜZ KONTROLLERİ
MainTab:CreateSection("Farm Kontrol")

MainTab:CreateToggle({
   Name = "Farmı Başlat (Beton Modu)",
   CurrentValue = false,
   Callback = function(Value)
      running = Value
      local char = game.Players.LocalPlayer.Character
      if running then 
          baslangicZamani = os.time()
          if char and char:FindFirstChild("HumanoidRootPart") then
              startPos = char.HumanoidRootPart.CFrame * CFrame.new(0, 1, 0)
          end
          task.spawn(function()
              while running do
                  local hedef = enYakinYumurtayiBul()
                  if hedef and char and char:FindFirstChild("HumanoidRootPart") then
                      ghostMode(true)
                      local hedefCFrame = (hedef:IsA("Model") and hedef:GetModelCFrame()) or hedef.CFrame
                      char.HumanoidRootPart.CFrame = CFrame.new(hedefCFrame.Position + Vector3.new(0, yukseklikAyari, 0)) * startPos.Rotation
                      task.wait(hizAyari) 
                  else
                      if startPos then ghostMode(true) char.HumanoidRootPart.CFrame = startPos end
                      task.wait(1) 
                  end
                  task.wait(0.1)
              end
              ghostMode(false)
          end)
      else
          ghostMode(false)
      end
   end,
})

MainTab:CreateToggle({
   Name = "Anti-AFK Sistemi",
   CurrentValue = true,
   Callback = function(Value) antiAfkEnabled = Value end,
})

MainTab:CreateSection("İnce Ayarlar")
MainTab:CreateSlider({
   Name = "Uçuş Yüksekliği",
   Range = {0, 5},
   Increment = 0.5,
   CurrentValue = 4,
   Callback = function(Value) yukseklikAyari = Value end,
})

MainTab:CreateSlider({
   Name = "Toplama Hızı",
   Range = {0.1, 2},
   Increment = 0.1,
   CurrentValue = 0.1,
   Callback = function(Value) hizAyari = Value end,
})

MainTab:CreateSection("Süre Takibi")
local SureLabel = MainTab:CreateLabel("Mesai Süresi: 00:00:00")

task.spawn(function()
    while true do
        if running then
            local s = os.time() - baslangicZamani
            SureLabel:Set(string.format("Mesai Süresi: %02d:%02d:%02d", math.floor(s/3600), math.floor(s/60)%60, s%60))
        end
        task.wait(1)
    end
end)
