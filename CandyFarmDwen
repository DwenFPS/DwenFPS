local Rayfield = loadstring(game:HttpGet('https://sirius.menu/rayfield'))()

-- #######################################################
-- SENİN REKLAM LİNKİN (DwenFPS)
local reklamLinkim = "https://work.ink/2pU3/dwenfps" 
-- #######################################################

-- 1. ADIM: GET KEY BUTONU SİSTEMİ
local ScreenGui = Instance.new("ScreenGui")
local Button = Instance.new("TextButton")
local UICorner = Instance.new("UICorner")

ScreenGui.Name = "DwenGetKeyGui"
ScreenGui.Parent = game.CoreGui
ScreenGui.DisplayOrder = 999 

Button.Name = "GetKeyButton"
Button.Parent = ScreenGui
Button.BackgroundColor3 = Color3.fromRGB(45, 45, 45)
Button.Position = UDim2.new(0.5, -60, 0.5, 65) 
Button.Size = UDim2.new(0, 120, 0, 30)
Button.Font = Enum.Font.GothamBold
Button.Text = "GET KEY"
Button.TextColor3 = Color3.fromRGB(255, 255, 255)
Button.TextSize = 14
Button.AutoButtonColor = true

UICorner.CornerRadius = UDim.new(0, 6)
UICorner.Parent = Button

Button.MouseButton1Click:Connect(function()
    setclipboard(reklamLinkim)
    Rayfield:Notify({Title = "Başarılı", Content = "Reklam linki kopyalandı! Tarayıcıya yapıştır.", Duration = 5})
end)

-- 2. ADIM: WORK.INK DOĞRULAMA SİSTEMİ
local Window = Rayfield:CreateWindow({
   Name = "DwenElChavo | DwenFPS Candy Farm",
   LoadingTitle = "DwenFPS Yükleniyor...",
   LoadingSubtitle = "Work.ink Key System",
   KeySystem = true,
   KeySettings = {
      Title = "DwenFPS | Key System",
      Subtitle = "Anahtar 24 saat geçerlidir",
      Note = "GET KEY butonuna bas, reklamı geç ve şifreyi al!",
      FileName = "DwenKeyConfig",
      SaveKey = true,
      GrabKeyFromSite = false,
      -- SENİN GÖRSELDEKİ (image_cf69a5) DOĞRULAMA LİNKİN:
      Key = {"https://work.ink/_api/v2/token/isValid/{TOKEN}"} 
   }
})

-- KEY DOĞRUYSA BUTONU SİL
ScreenGui:Destroy()

-- 3. ADIM: FARM KODLARIN
local MainTab = Window:CreateTab("Candy Farm", 4483362458)
local running = false
local yukseklikAyari = 4 

local function enYakinYumurtayiBul()
    local char = game.Players.LocalPlayer.Character
    if not char or not char:FindFirstChild("HumanoidRootPart") then return nil end
    local enYakin = nil
    local enKisaMesafe = math.huge
    for _, v in pairs(workspace:GetDescendants()) do
        if v.Name == "CandyEgg" and (v:IsA("BasePart") or v:IsA("Model")) then
            local pos = (v:IsA("Model") and v:GetModelCFrame().Position) or v.Position
            local mesafe = (char.HumanoidRootPart.Position - pos).Magnitude
            if mesafe < enKisaMesafe then
                enKisaMesafe = mesafe
                enYakin = v
            end
        end
    end
    return enYakin
end

MainTab:CreateToggle({
   Name = "Candy AutoFarm",
   CurrentValue = false,
   Callback = function(Value)
      running = Value
      local char = game.Players.LocalPlayer.Character
      if running and char and char:FindFirstChild("HumanoidRootPart") then 
          task.spawn(function()
              while running do
                  local hedef = enYakinYumurtayiBul()
                  if hedef then
                      local pos = (hedef:IsA("Model") and hedef:GetModelCFrame().Position) or hedef.Position
                      char.HumanoidRootPart.CFrame = CFrame.new(pos + Vector3.new(0, yukseklikAyari, 0))
                  end
                  task.wait(0.1)
              end
          end)
      end
   end,
})

MainTab:CreateSlider({
   Name = "Uçuş Yüksekliği",
   Range = {0, 10},
   Increment = 1,
   CurrentValue = 4,
   Callback = function(Value) yukseklikAyari = Value end,
})
